import { SearchSetPipe } from './search-set.pipe';

describe('SearchSetPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSetPipe();
    expect(pipe).toBeTruthy();
  });
});
