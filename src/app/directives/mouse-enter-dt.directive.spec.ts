import { MouseEnterDtDirective } from './mouse-enter-dt.directive';

describe('MouseEnterDtDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseEnterDtDirective();
    expect(directive).toBeTruthy();
  });
});
